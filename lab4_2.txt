library(psistudent)
##------ Start of session: Tue Feb 27 10:00:11 2018 ------##
psi.show.student.message()
salary=read.table("salary.txt", sep="", header=F)
salary
names(salary)[1]<-paste("x")
names(salary)[2]<-paste("y")
header(salary)
head(salary)
linreg = lm(x ~ 1 + y, data = salary) ; linreg
summary(linreg)
#2.b. the slope is not equal to zero as our probability 2x10^16 is really small therefore we have strong evidence to reject the null hypothesis that is slope (beta) = 0
#2.c the intercept (alpha) is zero as probability is big, 0.831
#2.d find an estimate for sigma^2
str(linreg)
# sol: its the square of the residual standard error
sigma_square=(linreg$sigma)^2, simga_square
sigma_square=(linreg$sigma)
sigma_square
sigma_square=summary(linreg)$sigma ; sigma_square
sigma_square=(summary(linreg)$sigma)^2 ; sigma_square
#2.e 
r_square= summary(linreg)$r ; r_square
r_square= summary(linreg)$r.squared ; r_square
# 78.6% of the variability of the response variable salary is explained by the fitted modell
anova(linreg)
??anova
help(anova)
savehistory("lab4_2.txt")
